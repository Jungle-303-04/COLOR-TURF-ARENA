apiVersion: v1
kind: Service
metadata:
  name: paint-arena-frontend
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: paint-arena-frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080

